@extends('layouts.app') @section('content')<section class="auth"><p class="eyebrow">SELAMAT DATANG KEMBALI</p><h1>Masuk ke ruang baca.</h1><form class="form-card" method="POST" action="{{ route('login') }}">@csrf<label>Email<input type="email" name="email" value="{{ old('email') }}" required autofocus></label><label>Password<input type="password" name="password" required></label><label><input type="checkbox" name="remember"> Ingat saya</label><button class="button">Masuk <span>-></span></button>@if($errors->any())<p class="error">{{ $errors->first() }}</p>@endif</form></section>@endsection
