@extends('layouts.app') @section('content')<section class="page-head"><p class="eyebrow">KOLEKSI</p><h1>Katalog buku</h1><p>Temukan bacaan berikutnya dari koleksi kami.</p></section><section class="catalog"><form class="filters" method="GET"><input name="q" value="{{ request('q') }}" placeholder="Cari judul atau penulis..."><select name="kategori"><option value="">Semua kategori</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(request('kategori')==$category->id)>{{ $category->nama_kategori }}</option>@endforeach</select><label><input type="checkbox" name="tersedia" value="1" @checked(request('tersedia'))> Tersedia</label><button class="button small">Cari</button></form><div class="book-grid">@forelse($books as $book)<a class="book-card" href="{{ route('books.show',$book) }}"><img src="{{ $book->cover_url }}" alt="{{ $book->judul }}"><div><small>{{ $book->category->nama_kategori }}</small><h3>{{ $book->judul }}</h3><p>{{ $book->penulis }}</p><span class="rating">★ {{ number_format($book->rating,1) }} · {{ $book->stok }} tersedia</span></div></a>@empty<p>Belum ada buku yang cocok.</p>@endforelse</div>{{ $books->links() }}</section>@endsection
