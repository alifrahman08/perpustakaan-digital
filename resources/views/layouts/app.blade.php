<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ $title ?? 'Pustaka Digital' }}</title><link rel="preconnect" href="https://fonts.googleapis.com"><link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&family=Playfair+Display:wght@600;700&display=swap" rel="stylesheet">@vite(['resources/css/app.css','resources/css/redesign.css','resources/js/app.js'])</head><body><nav class="nav"><a class="brand" href="{{ route('home') }}"><span>PD</span>Pustaka Digital</a><div class="navlinks"><a href="{{ route('books.index') }}">Katalog</a><a href="{{ route('about') }}">Tentang</a>@auth <a href="{{ auth()->user()->isAdmin() ? route('admin.dashboard') : route('dashboard') }}">Dashboard</a><form method="POST" action="{{ route('logout') }}">@csrf<button>Keluar</button></form>@else<a href="{{ route('login') }}">Masuk</a><a class="button small" href="{{ route('register') }}">Daftar</a>@endauth</div></nav>@if(session('success'))<div class="flash success">{{ session('success') }}</div>@endif @if(session('error'))<div class="flash error">{{ session('error') }}</div>@endif<main>@yield('content')</main><footer><strong>Pustaka Digital</strong><span>Ruang baca modern untuk semua.</span></footer></body></html>
