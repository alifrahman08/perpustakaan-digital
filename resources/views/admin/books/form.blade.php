@extends('layouts.app') @section('content')<section class="page-head"><p class="eyebrow">ADMIN / KOLEKSI</p><h1>{{ $book->exists ? 'Edit buku' : 'Tambah buku' }}</h1></section><form class="form-card" method="POST" action="{{ $book->exists ? route('admin.books.update',$book) : route('admin.books.store') }}">@csrf @if($book->exists) @method('PUT') @endif<div class="form-grid"><label>Judul<input name="judul" value="{{ old('judul',$book->judul) }}" required></label><label>Penulis<input name="penulis" value="{{ old('penulis',$book->penulis) }}" required></label><label>Kategori<select name="kategori_id">@foreach($categories as $category)<option value="{{ $category->id }}" @selected(old('kategori_id',$book->kategori_id)==$category->id)>{{ $category->nama_kategori }}</option>@endforeach</select></label><label>Penerbit<input name="penerbit" value="{{ old('penerbit',$book->penerbit) }}"></label><label>Tahun terbit<input type="number" name="tahun_terbit" value="{{ old('tahun_terbit',$book->tahun_terbit) }}"></label><label>Stok<input type="number" name="stok" value="{{ old('stok',$book->stok ?? 1) }}" min="0"></label><label>URL cover<input name="cover_image" value="{{ old('cover_image',$book->cover_image) }}"></label><label>Jumlah halaman<input type="number" name="jumlah_halaman" value="{{ old('jumlah_halaman',$book->jumlah_halaman) }}"></label></div><label>Sinopsis<textarea name="deskripsi">{{ old('deskripsi',$book->deskripsi) }}</textarea></label><button class="button">Simpan buku</button></form>@endsection
