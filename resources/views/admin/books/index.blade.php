@extends('layouts.app') @section('content')<section class="page-head inline"><div><p class="eyebrow">ADMIN / KOLEKSI</p><h1>Kelola buku</h1></div><a class="button small" href="{{ route('admin.books.create') }}">+ Tambah buku</a></section><section class="section table-wrap"><table><thead><tr><th>Judul</th><th>Kategori</th><th>Stok</th><th>Aksi</th></tr></thead><tbody>@foreach($books as $book)<tr><td><b>{{ $book->judul }}</b><small>{{ $book->penulis }}</small></td><td>{{ $book->category->nama_kategori }}</td><td>{{ $book->stok }}</td><td><a href="{{ route('admin.books.edit',$book) }}">Edit</a> <form class="inline-form" method="POST" action="{{ route('admin.books.destroy',$book) }}">@csrf @method('DELETE')<button>Hapus</button></form></td></tr>@endforeach</tbody></table>{{ $books->links() }}</section>@endsection
