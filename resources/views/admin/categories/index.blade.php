@extends('layouts.app') @section('content')<section class="page-head inline"><div><p class="eyebrow">ADMIN / TAKSONOMI</p><h1>Kelola kategori</h1></div><a class="button small" href="{{ route('admin.categories.create') }}">+ Tambah kategori</a></section><section class="section table-wrap"><table><thead><tr><th>Nama</th><th>Slug</th><th>Jumlah buku</th><th>Aksi</th></tr></thead><tbody>@foreach($categories as $category)<tr><td>{{ $category->nama_kategori }}</td><td>{{ $category->slug }}</td><td>{{ $category->books_count }}</td><td><a href="{{ route('admin.categories.edit',$category) }}">Edit</a> <form class="inline-form" method="POST" action="{{ route('admin.categories.destroy',$category) }}">@csrf @method('DELETE')<button>Hapus</button></form></td></tr>@endforeach</tbody></table>{{ $categories->links() }}</section>@endsection
