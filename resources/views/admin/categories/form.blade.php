@extends('layouts.app') @section('content')<section class="page-head"><p class="eyebrow">ADMIN / TAKSONOMI</p><h1>{{ $category->exists ? 'Edit kategori' : 'Tambah kategori' }}</h1></section><form class="form-card" method="POST" action="{{ $category->exists ? route('admin.categories.update',$category) : route('admin.categories.store') }}">@csrf @if($category->exists) @method('PUT') @endif<label>Nama kategori<input name="nama_kategori" value="{{ old('nama_kategori',$category->nama_kategori) }}" required></label><label>Deskripsi<textarea name="deskripsi">{{ old('deskripsi',$category->deskripsi) }}</textarea></label><button class="button">Simpan kategori</button></form>@endsection
